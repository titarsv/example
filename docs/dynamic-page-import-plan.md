# План: ускорение посадки динамических страниц (каталог/товар/поиск/блог/статья/чекаут/спасибо/404)

Дата фиксации: 2026-08-13 (архитектура пересмотрена в тот же день — см. правку ниже)

## Контекст и отличие от [[project-page-import-feature]]

`Modules/PageImport` (см. `docs/page-import-plan.md`) решает задачу «вытащить контент из статичной
вёрстки донора в поля ACF-шаблона» — годится для действительно статичных информационных страниц
(`about`/`faq`/`contacts`/…), где весь контент — авторский текст без бэкенд-логики.

Каталог/товар/поиск/блог/статья/чекаут/страница благодарности/404 — принципиально другая задача.
Контента для извлечения там нет: это уже рабочая динамическая логика в фиксированных файлах темы
(`resources/themes/{theme}/views/public/catalog.blade.php` и т.п.), рендерящая реальные `Product`/
`Category`/`Blog`/`Order` из БД. Донор просто рисует **другой дизайн той же самой логики**.
Значит, задача верстальщика тут — не «завести поля», а **пересадить разметку/CSS-классы донора
вокруг уже существующих `@foreach`/`{{ }}`-выражений**, не сломав саму логику. Это структурный
transplant, а не извлечение контента.

## Решение по архитектуре (зафиксировано с пользователем): отдельная точка входа, оркестратор → новая тема

Не расширяем существующий раздел «Импорт страниц» — это **отдельный** пункт админки со своим
потоком:

1. Загружается тот же ZIP-архив (донор сдаёт статичные и динамические страницы вместе, одной
   сборкой).
2. **Оркестратор** идёт по каждому найденному html-файлу и **классифицирует тип страницы**
   (`static` / `catalog` / `product` / `search` / `blog` / `article` / `checkout` / `thanks` —
   таксономия ниже).
3. Оркестратор создаёт **новую тему** тем же механизмом, что уже есть в проекте —
   `php artisan theme:make {name} --from=base` (см. `docs/themes.md`) — полная копия `base`.
4. Для каждой классифицированной страницы генерируется файл **внутри этой новой темы**
   (`resources/themes/{new-theme}/views/public/...`), заменяя ровно то, что отличается от
   `base`. Всё остальное наследуется от `base` за счёт уже готового view-fallback механизма
   (`config/view.php`) — ничего специально доделывать для этого не нужно, это ровно то, для чего
   и была построена система тем.

**Почему это принципиально безопаснее** первой версии плана (там результат транспланта нужно было
класть как ревизию поверх ЖИВОЙ активной темы, с ручным гейтом через `RecordsTemplateRevisions`):
новая тема **не активна**, пока кто-то явно не выставит `ACTIVE_THEME` — сайт всё время сборки и
ревью продолжает работать на текущей теме без единого изменения. Реальный прод-чекаут, реальный
каталог — не тронуты структурно, а не потому что мы аккуратно поставили gate. Пункт про
review-gate из первой версии плана снимается — эта архитектура закрывает ту же потребность
надёжнее.

Статические страницы архива (about-подобные) идут туда же, тем же путём, что и сейчас
(`PageBuilder`/`SchemaBuilder`), с одним изменением — целевая тема параметризуется, а не жёстко
берётся из `theme_relative_path()` (которая сейчас всегда бьёт в активную тему).

## Источник архива: не `dist/`, а исходники верстальщика (`app/`)

Раз мы всё равно создаём новую тему — имеет смысл принимать не скомпилированный `dist/`, а полный
проект верстальщика (`app/` + `package.json`/`webpack.config.js`, без `node_modules`), чтобы сразу
разложить шрифты/картинки/scss/js по местам, а не только html. Изучил оба реальных донора
(`C:\Development\Layouts\vza`, `C:\Development\Layouts\proper-loud`) — оба собраны на одном и том же
boilerplate (`package.json`: `"triplefork-project-builder"`), той же линии, что и собственный
`package.json` этого проекта (`"project-builder"`). Структура source предсказуема и одинакова у
обоих:

```
app/
  app.js                          — единая точка входа JS донора (шапка/меню/общие виджеты — нам не нужна)
  assets/
    templates/layouts/{name}.html — по файлу на страницу, имя = слаг (strains.html, catalog.html…)
    templates/parts/{header,footer}.html — общий чехол донора, не нужен (своя тема даёт header/footer)
    stylesheets/
      app.scss                    — единая точка входа SCSS донора
      _variables.scss, _mixin.scss, _vendor.scss, … — общее для всех страниц донора
      _{name}.scss                — SCSS **конкретно под каждую страницу**, то же имя, что и .html
    images/*, fonts/*             — плоские папки, чистые оригинальные имена файлов
```

Практические следствия (проверено на реальных файлах, не гипотеза):

- `app/assets/templates/layouts/{name}.html` — прямая замена `dist/{name}/index.html`, `<main>`
  внутри — обычный HTML. `<include src="...header/footer.html">` (build-time директива
  `posthtml-include`) встречается ТОЛЬКО снаружи `<main>` у обоих доноров — `ArchiveParser` работает
  без изменений, `DOMDocument` просто игнорирует незнакомый тег.
- Картинки в source ссылаются чистыми относительными путями с оригинальными (не захэшированными
  webpack'ом) именами — та же `resolveAssetPath()`, что уже есть, результат только чище.
- SCSS уже разложен по страницам 1:1 по имени — `_strains.scss` ↔ `strains.html`. Механическое
  сопоставление по имени, ИИ не нужен.

**Реальная коллизия, а не гипотетическая**: donor's `_variables.scss` определяет `$black`/`$dark`/…,
тема проекта — свои `$primary`/`$dark`/… в `resources/themes/base/scss/_variables.scss`. Оба
используют `$dark`. SCSS `@import` — не модульная система, переменные глобальны, «последняя запись
побеждает». Слить donor'ские `_variables.scss`/`_mixin.scss` в общий `app.scss` темы — значит тихо
перезаписать `$dark` для всех страниц темы после точки импорта.

**Решение — не мержить в общий `app.scss`, отдельный Mix-entry на страницу**:

- Donor-файлы для сгенерированной страницы (`_{name}.scss` + нужные ей `_variables.scss`/
  `_mixin.scss`, например ради `@include bp(...)`) копируются в `resources/themes/{new-theme}/scss/
  imported/{name}/` — отдельная подпапка на страницу, чтобы donor-переменные РАЗНЫХ импортов тоже не
  сталкивались друг с другом.
- Компилируется отдельным Mix-entry в `public/themes/{theme}/css/imported/{name}.css`, подключается
  `<link>`'ом только в сгенерированном шаблоне ЭТОЙ страницы — не участвует в общем `app.css`. Ноль
  риска коллизии переменных — физически разные CSS-файлы. `webpack.mix.js` дополняется: перебор
  `${themeSrc}/scss/imported/*` → по `.sass()`-вызову на каждую найденную подпапку (тот же паттерн,
  что уже есть у `if (fs.existsSync(...))`-проверок для images/fonts).
- Картинки/шрифты — копируются без проблем, `mix.copyDirectory()` уже рекурсивный. Кладём в
  `images/imported/`, `fonts/imported/` (подпапка — чтобы не перезаписать `favicon.png`/логотип темы
  одноимённым файлом донора).
- **JS — решение пересмотрено** (изначально в этом плане было «не автоматизируем»): найден точный,
  уже работающий прецедент — сравнил `app/app.js` донора (идентичный у vza/proper-loud) с
  `C:\OSPanel\home\nnn-site.lh\public\resources\js\app.js`, где ровно эта задача уже решена на
  реальном проекте той же линии. Схема:

  | Донорский `app.js` | `nnn-site.lh` версия |
  |---|---|
  | `require('jquery'); require('bootstrap-sass');` | `require('./bootstrap')` — свой jQuery + настоящий Bootstrap 5, не `bootstrap-sass` донора |
  | `require('./modules/forms')` и т.п. | та же папка, просто скопирована на новое место, путь не меняется |
  | `require('../node_modules/sumoselect/...')` | `require('../../node_modules/sumoselect/...')` — глубина `../` пересчитана под новое расположение файла |
  | `import './assets/stylesheets/app.scss';` | убрано — SCSS не через JS-импорт (см. отдельный Mix-entry выше) |
  | — | в конце файла: `require('./custom.js')`, который сам в конце делает `require('./larchik/filter'); require('./larchik/cart'); require('./larchik/checkout');` |

  Вся UI-логика донора (слайдеры/попапы/аккордеон/шапка) остаётся в `app.js` как есть, только с
  актуализированными путями; бизнес-логика (корзина/фильтры/чекаут) — отдельными файлами,
  дописанными `require`'ом в конец. Конфликта с UI-логикой донора нет: её обработчики навешаны на
  donor-классы, которых в унаследованных от `base` header/footer нет — просто не сработают, а не
  сломают что-то. JS-модули (в отличие от SCSS-переменных) не делят область видимости через
  `require()`, поэтому объединение в один `js/app.js` на тему безопасно — отдельный Mix-entry на
  страницу, как для CSS, тут не нужен.

  **Предпосылка (разовый рефакторинг, как и с 404 ниже)**: в текущем `resources/themes/base/js/
  app.js` этого проекта корзина/избранное/сравнение/фильтры каталога/чекаут ещё не вынесены
  отдельно — всё в одном файле (776 строк). Проверено: весь этот код навешан через
  `$(document).on(...)` на классы/id, самодостаточен, ни от чего другого в файле не зависит —
  вынос в `resources/themes/base/js/shop/{cart,wishlist,compare,filters,checkout}.js` + один
  `require('./shop')` в конце `app.js` — чистое перемещение кода без риска. После этого рефакторинга
  оркестратор для `app.js` новой темы: donor's `app/app.js` как база → вендорная шапка меняется на
  уже готовый блок инициализации jQuery/Bootstrap из текущего `app.js` темы → `app/modules/` →
  `js/modules/` (пути не меняются) → глубина `../` до `node_modules` пересчитывается (с `app/app.js`
  был 1 уровень, из `resources/themes/{theme}/js/app.js` будет 4 — как уже сейчас у `app.scss`) →
  `import '...app.scss'` убирается → в конец дописывается `require('.../shop')`.

Существующий `Modules/PageImport` (dist-based, только статика) не меняется — это отдельный,
самостоятельный оркестратор со своим форматом загрузки.

## Классификация типа страницы

Два прохода, от дешёвого к дорогому:

1. **Эвристика по пути/имени файла** — быстро, детерминированно, бесплатно. Словарь строится по
   уже реально виденным на обоих протестированных донорах именам (`page-import-plan.md`, «Проверка
   на втором доноре»): `catalog`/`shop`, `*-product`/`product`, `search-results`, `blog`,
   отдельный слаг статьи под `blog/`, `checkout`/`checkout-*` (у proper-loud их было три варианта:
   `checkout`, `checkout-cryptocurrency`, `checkout-properloudplay`), `thank-you`/`thanks`, `404`.
2. **Fallback — лёгкая ИИ-классификация** только для того, что эвристика не разобрала уверенно:
   новый узкий метод (например `classifyPageType(html, context): ?string`), возвращает ОДНО
   значение из фиксированного enum'а, а не свободный разбор — на порядок дешевле, чем
   `analyzePageMarkup()`/будущий `mapDynamicPageSlots()`.
3. **Не разобрано ни эвристикой, ни ИИ → трактуем как `static`.** Это безопасный дефолт: худший
   случай — чуть неидеальная ACF-страница, которая всё равно черновик до публикации, а не что-то,
   что может задеть динамическую логику.

**`checkout`/`thanks` классифицируются, но НИКОГДА не транспланируются.** Оркестратор узнаёт их и
осознанно НЕ генерирует для них файл — так же, как сейчас `PageBuilder::SKIP_NAMES` уже пропускает
`404`/`403`/`500`/`error` для страниц-заглушек, только здесь это не «пропустить», а «распознать и
явно отказаться трогать», с записью в лог импорта. Раз файла в новой теме нет — она наследует
РЕАЛЬНЫЙ работающий `checkout.blade.php`/`thanks.blade.php` из `base` через тот же fallback.
Ошибка классификации (перепутали checkout с чем-то другим) в худшем случае просто не сгенерирует
файл там, где могла бы — не создаёт риска сломать оплату, потому что генерация для этих типов в
принципе не предусмотрена.

## Решено: `404` заводится в тему через тонкую стабильную оболочку

Изначально казалось, что `404` не участвует в системе тем и требует спецобработки/отдельного гейта.
На деле `config/view.php` не различает `public.*` от остального — он просто
добавляет `resources/themes/{active}/views`/`resources/themes/{fallback}/views` в общий
`view.paths`, так что ЛЮБОЕ имя вида `errors.404` резолвится по тем же путям, что и `public.catalog`.
Более того, текущий `resources/views/errors/404.blade.php` УЖЕ зависит от темы —
`@extends('public.layouts.main')` тянет весь хедер/футер/меню через тот же механизм. Реально
раннего резолва «до полной загрузки приложения» (ради которого `errors/*` вынесены из тем по
`docs/themes.md`) это требует не для `404`, а для `503.blade.php` (чистый статический HTML,
без единого `@extends` — на случай, когда Laravel едва поднимается) и отчасти `403` (свой отдельный
admin-layout, не публичная тема).

**Решение**: выносим сам контент 404 в тему, оставляя `errors/404.blade.php` тонкой, навсегда
стабильной оболочкой, которую импорт больше не трогает:

```blade
{{-- resources/views/errors/404.blade.php — не меняется импортом, стабильная инфраструктура --}}
@extends('public.layouts.main')
@section('title','Error 404')
@section('content')
    @include('public.errors.404_content')
@endsection
```

`public.errors.404_content` — обычный themed-partial, живёт в `resources/themes/base/views/public/
errors/404_content.blade.php` (содержит сегодняшний хардкод) и переопределяется в
`resources/themes/{new-theme}/...` точно так же, как любой другой файл темы. Оркестратор для типа
`404` пишет туда — никакой спецобработки, никакой отдельной кнопки «применить прод немедленно»:
`404` переходит в тир полной автоматизации наравне с blog/article, полностью внутри песочницы новой
темы. Новых рисков это не добавляет — 404 и так уже зависит от `public.layouts.main`/темы, это
только формализует то, что фактически уже так работает.

## Обновлённая таблица риска

Раз ничего не уходит в прод до явного переключения темы, риск сместился с «не автопубликовать» на
«не генерировать вовсе там, где цена ошибки — сломанная бизнес-логика, а не просто кривая вёрстка»:

| Группа | Страницы | Что делает оркестратор |
|---|---|---|
| Полная автоматизация | **blog, article, 404** | Классифицирует и транспланирует в новую тему без исключений — нет денег/логики заказа, худший случай — кривая страница в неактивной теме (404 — после выноса контента в `public.errors.404_content`, см. выше) |
| Автоматизация + внимательный ревью перед активацией темы | **catalog, product, search** | Транспланирует (репитер карточки товара — через уже вынесенный общий partial `public.layouts.product`, используется и `catalog`, и `search`), но перед переключением `ACTIVE_THEME` на новую тему — обязательный визуальный просмотр (сайт можно открыть на новой теме локально/на стейджинге ещё ДО переключения основного окружения) |
| Классифицируется, но НИКОГДА не транспланируется | **checkout, thanks** (и варианты типа `checkout-cryptocurrency`) | Файл в новой теме не создаётся — наследуется рабочий `base`. Из автоматизации сюда можно унести только перенос картинок/иконок донора в медиатеку (`AssetImporter` уже умеет), без записи шаблона |

## Порядок реализации (предлагаемый)

0. ✅ **Оркестратор-каркас**: новый пункт админки (`Modules\ThemeImport`, отдельный от
   `Modules\PageImport`), приём архива **с исходниками верстальщика** (`app/`, не `dist/`),
   эвристическая классификация по имени/пути, `theme:make`-эквивалент (`ThemeForker`),
   маршрутизация `static`-страниц через существующий `PageBuilder`/`SchemaBuilder` (целевая тема
   параметризована — `PageBuilder::build()`/`writeTemplate()`/`uniqueTemplateName()` теперь
   принимают `?string $theme`), раскладка `images`/`fonts`/`scss` по местам (`AssetPlacer` —
   изолированный Mix-entry на страницу) + `webpack.mix.js` дополнен динамическим перебором
   `scss/imported/*`, сборка `js/app.js` новой темы из donor's `app/app.js` (`JsAppAssembler`).
   **Живой прогон на реальном доноре** (`proper-loud`, полный `app/`, через настоящий HTTP-путь
   контроллера → очередь): 24 страницы классифицированы верно, тема создана, 12 static-страниц
   собраны, 0 ошибок, коллизии имён с существующими шаблонами (`about`→`about-import-2` и т.д.)
   разрешены корректно, SCSS разложен, `js/app.js` собран без утечек `.scss`-импорта. Все тестовые
   данные (тема, 12 страниц, 62 медиафайла, запись импорта) удалены после проверки.
   **Побочная находка (окружение, не код)**: при быстрых правках job-класса воркер иногда падал с
   `Job is incomplete class` — `opcache.revalidate_freq=2` кэширует байткод на 2 секунды, воркер
   иногда видел устаревшую версию файла. Не баг кода — просто держать в уме при быстрой итерации
   джоб в этом окружении (`php -d opcache.enable_cli=0 artisan queue:work` как обход на время
   разработки).
0.1. ✅ **Подготовительный рефакторинг `resources/themes/base/js/app.js`**: корзина/избранное/
   сравнение/фильтры каталога/чекаут вынесены в `js/shop/{cart,wishlist,compare,filters,
   checkout}.js` + `require('./shop')` в конце `app.js`. Заодно (обнаружено по ходу шага 0)
   презентационная логика самой `base` (FAQ-таб/форма отзыва/ajax-формы/слайдер рекомендаций)
   вынесена в `js/site.js` — без этого страницы, унаследованные из `base` в собранной оркестратором
   теме, теряли интерактивность (их разметка не переопределяется новой темой, а `js/app.js`
   новой темы — целиком donor's код, не знающий про `.js-faq-tab` и т.п.). `ThemeForker` чистит
   унаследованные при `theme:make` копии `js/shop`/`js/site.js` из новой темы (не используются —
   `JsAppAssembler` всегда ссылается на `base` напрямую).
1. ✅ **ИИ-классификация как fallback**: `AiServiceInterface::classifyPageType()` (+ общий трейт
   `BuildsPageTypePrompt`, Gemini/Ollama реализации, без `withRetries()` — вызов некритичный, при
   неудаче просто `static`). `PageTypeClassifier` разделён на `matchByName()` (только явные
   совпадения, `null` = «не уверена») и `classify()` (старое поведение с дефолтом, для мест без
   ИИ). Оркестратор: `matchByName()` → при `null` пробует `classifyPageType()` → при неудаче
   дефолт `static`; результат (`classified_by: heuristic|ai|default|skip`) сохраняется в `pages`/
   `built_pages` и показывается в экране ревью значком.
   **Проверено дважды**: точечно — реальный вызов Gemini на настоящем HTML (`about.html`/
   `catalog.html`/`product.html` донора) с намеренно обманчивой подсказкой имени файла
   (`page-42`) — все три классифицированы верно ПО КОНТЕНТУ, не по имени; и целиком через
   реальный `handle()` джобы — мини-архив из 2 страниц с именами, которые эвристика не
   распознаёт (`page-one`=about, `page-two`=catalog), оба ушли на ИИ-фоллбек, оба
   классифицированы верно, `page-one` (static) корректно собран. Тестовые данные удалены.
2. ✅ **blog/article транспланter**: общий узкий AI-метод `mapDynamicPageSlots()` (+ трейт
   `BuildsDynamicPageSlotsPrompt`, Gemini/Ollama) + фиксированный словарь слотов на каждый тип
   (`DynamicSlotVocabulary`) + ручная карта `slot → реальное Blade-выражение` (`BladeExpressionMap`,
   написана один раз разработчиком по уже рабочему `blog.blade.php`/`article.blade.php`,
   переиспользуется на каждом следующем архиве). Сплайсинг (`DynamicPageTransplanter`) переиспользует
   DOM-механику `SchemaBuilder` — общие куски (поиск инстансов repeater'а, резолв простых
   селекторов, сериализация, условная замена `<img>`) вынесены в трейт `ResolvesDomNodes`.
   Все типы слотов (repeater-дети и одиночные leaves) резолвятся через `selector_path` от найденного
   ИИ узла, а не через сравнение транскрибированного моделью текста — значение для сплайса всегда
   берётся из САМОГО резолвленного DOM-узла (гарантированно точное совпадение при обратном поиске).
   **Живое тестирование (несколько раундов, все баги найдены и исправлены по ходу)**:
   - Длинный wysiwyg-текст (`article.html`, тело статьи) не сплайсился — модель слегка
     переформатировала пробелы при транскрипции `value`, точное строковое совпадение срывалось.
     Фикс: резолв через `selector_path`, а не через `value`.
   - Заголовок статьи иногда сплайсился в ЧУЖОЕ место — на `article.html` то же самое название
     дословно повторяется в виджете «Latest news», и транскрипция модели точнее совпала с чужим
     повтором (другая пунктуация донора), чем с настоящим `<h1>`. Тот же фикс (resolve по узлу)
     снял и эту проблему заодно.
   - Архитектурный конфликт: `ArchiveParser::parseHtmlFile()` уже вырезает первый `<h1>` донора ДО
     того, как транспланter видит контент (см. «Находка 4» в `page-import-plan.md`, рассчитано на
     static-страницы) — слот `title` для `article` физически не мог найти узел. Фикс: слот `title`
     убран из словаря `article`, `<h1>{{ $seo->name }}</h1>` вставляется автоматически через
     `wrap()`, как и у `blog`/static.
   - На `blog.html` слот `title` в детях repeater'а не сплайсился, хотя `image`/`date`/`excerpt`
     сплайсились корректно — модель вопреки прямому запрету в промпте иногда возвращает составной
     селектор (`"div.blog-item__info span.blog-item__title"`, несколько токенов через пробел)
     вместо простого. `resolveSimpleSelector()` изначально поддерживал только ОДИН токен и молча
     возвращал `null` на составных — тот же класс проблемы, что «осиротевшие поля» у
     `SchemaBuilder`. Фикс (в общем трейте `ResolvesDomNodes`, выгоден и static-страницам): резолвер
     расширен до цепочки простых токенов через пробел/`>` — каждое звено цепочки по-прежнему
     простое (тег/тег.класс/`:nth-of-type(N)`), просто резолвится по уровням вложенности, а не
     единым сложным селектором. Регрессия проверена синтетически (одиночные токены — как раньше).
   - **Полный прогон через реальный `handle()` джобы** (`proper-loud`, весь `app/`, `ThemeForker` →
     реальная тема): 24 страницы классифицированы, 9 static собраны через `PageBuilder`, `blog` и
     `article` оба собраны трансплантером (`status: created`), остальные типы (catalog/product/
     search/checkout/thanks) корректно помечены `not_implemented` (трансплантер для них — шаги 3–4),
     0 ошибок. Итоговые blade-файлы обеих динамических страниц проверены построчно — все слоты на
     месте (`{{ $article->name }}`, `{{ date(...) }}`, `{!! $article->excerpt !!}`, условная
     картинка, `{!! html_entity_decode($article->body) !!}`, `{{ $seo->name }}` в `<h1>`,
     `@include('public.layouts.pagination', ...)`), донорская декоративная разметка вне словаря
     слотов (статичная пагинация внизу `blog.html`, виджет «Latest news» на `article.html`) намеренно
     остаётся как есть — тот же принцип, что и у static-страниц. Все тестовые данные (9 страниц, 33
     медиафайла, тема, запись импорта) удалены, счётчики (`Page::count()`/`File::count()`) вернулись
     к базовой линии 7/2735.
3. ✅ (частично, v1) **catalog/search транспланter**: сокращённый объём, зафиксирован с
   пользователем перед реализацией — карточка товара в repeater'е (`public.layouts.product`, см.
   `resources/themes/base/views/public/layouts/product.blade.php`) сложный интерактивный partial
   (корзина/избранное/сравнение/quick-view, переиспользуется в catalog/search/similar/
   bought_together) — донорская вёрстка карточки структурно НЕ может дать эти хуки через простой
   сплайсинг текста (тот же вывод, что и у карточки товара в самом `product.html`). Решение:
   **компонентный повтор** — донорский инстанс карточки заменяется ЦЕЛИКОМ на
   `@include('public.layouts.product', ['product' => $product])`, донорская ОБЁРТКА инстанса
   (класс/grid-атрибуты) сохраняется — тот же принцип, что и `products_list.blade.php` оборачивает
   `@include` в свой `<div class="col">`. Новый ключ `repeat_component` в `BladeExpressionMap` (в
   отличие от `repeat_children` у blog) + соответствующая ветка в `DynamicSlotVocabulary` (`children`
   всегда `[]` — детей повтора разбирать не нужно, только сам факт повтора и его `selector_path`) +
   упрощённая ветка промпта в `BuildsDynamicPageSlotsPrompt` (не просит разбор полей одного
   инстанса). `DynamicPageTransplanter::spliceRepeat()` разделён на `spliceRepeatComponent()`
   (новая) и `spliceRepeatChildren()` (прежнее поведение blog, без изменений).
   Донорский сайдбар фильтров/сортировки (специфичный для каждого архива UI, не фиксированный
   словарь, как у blog/article) **сознательно не транспланируется в v1** — остаётся статичной
   декоративной вёрсткой донора, как и непокрытые словарём виджеты у blog/article. `product.html`
   (страница товара: варианты/атрибуты/отзывы/видео/похожие/корзина) **отложен целиком** — остаётся
   `not_implemented`, как checkout/thanks; вернуться отдельным шагом, когда будет 2–3 донора для
   сверки паттерна.
   `wrap()` расширен двумя точечными переопределениями под тип (нужны были реальными расхождениями
   в `catalog.blade.php`/`search.blade.php`, не гипотетически): `extends_params` — catalog передаёт
   `pagination`/`root_category` доп. параметрами в `@extends` (rel=prev/next в `<head>`, подсветка
   категории в шапке — как у рабочего `catalog.blade.php`); `open_graph` — у `search` **нет
   переменной `$seo` вовсе** (`ProductsController::search()` передаёt только `$products`/
   `$search_text`) — дефолтная секция `page_vars` (`$seo->meta_title` и т.п.) обвалилась бы
   `Undefined variable`, поэтому `search` целиком переопределяет `page_vars` через `open_graph`.
   Регрессия blog/article проверена побайтово (`wrap()` без переопределений даёт идентичный прежнему
   вывод).
   **Живой баг, пойманный до полного прогона**: `mapDynamicPageSlots()` иногда (не всегда —
   ловится статистически, не на каждом вызове) возвращал НЕВАЛИДНЫЙ JSON для `search` — голый
   `"repeat": {...}` без внешних `{}`. Причина — секция промпта для компонентного повтора (новая,
   короткая) показывала пример строго в виде `"repeat": {...}`, и модель иногда копировала его
   буквально как финальный ответ, не оборачивая в объект верхнего уровня. Фикс: все примеры в
   `BuildsDynamicPageSlotsPrompt` переписаны с явными оборачивающими `{}` (`{"repeat": {...}}`,
   `{"leaves": [...]}`), плюс явное заключительное правило запрещает голый фрагмент без `{}`.
   Проверено 5/5 подряд успешных валидных ответов после фикса (было 2/3 невалидных до).
   **Полный прогон через реальный `handle()` джобы** (`proper-loud`, весь `app/`): 17 страниц
   собрано (было 11 на шаге 2), 0 ошибок — `blog`/`article` (шаг 2, без регрессии),
   `catalog`/`search` (новое, шаг 3) все собраны. У этого донора ОДНОМУ типу `catalog` эвристика/
   ИИ сопоставили 4 разных файла (`catalog.html`, `city.html`, `index.html`, `strains.html`) и
   `search` — 2 файла (`search-results.html`, `search-results-empty.html`) — **известное
   ограничение**: у темы один route на тип, поэтому `views/public/{type}.blade.php` перезаписывается
   каждой последующей страницей того же типа, в теме остаётся только ПОСЛЕДНЯЯ обработанная
   (в этом прогоне — `strains.html`/`search-results-empty.html` соответственно). Само по себе не
   ломает файл (оба конечных blade скомпилированы Blade-компилятором без ошибок, `@foreach`/
   `@include` на месте), но по существу вопрос **более точной под-классификации** внутри
   `catalog`/`search` (например «это НАСТОЯЩАЯ страница каталога» против «страница с виджетом
   товаров сбоку») — открытый вопрос, не решается в v1, см. «Открытые вопросы».
   Итоговые blade-файлы (`catalog`/`search`) проверены построчно и Blade-компилятором
   (`app('blade.compiler')->compileString()` + `php -l` на скомпилированном PHP) — компилируются
   без ошибок у всех 4 типов (blog/article/catalog/search). Все тестовые данные (8 страниц, 13
   медиафайлов, тема, запись импорта) удалены, счётчики вернулись к базовой линии 7/2735.
4. ✅ **checkout/thanks**: только классификация + пропуск — уже полностью покрыто существующим
   кодом оркестратора (шаги 0–1: классификация даёт `checkout`/`thanks`, эти типы не входят ни в
   `TYPE_STATIC`, ни в `$transplantableTypes`, попадают в общую ветку `not_implemented` — файл
   шаблона не пишется, тема наследует рабочий `base`); scss/картинки/шрифты доноров всё равно
   раскладываются в тему целиком (`AssetPlacer`, не зависит от типа страницы) — новый код не
   понадобился. Опциональный перенос картинок конкретно checkout/thanks в медиатеку через
   `Modules\PageImport\Services\AssetImporter` **сознательно не сделан**: у этих страниц не будет
   ни Page-записи, ни сгенерированного шаблона, куда могла бы пригодиться ссылка на медиатеку —
   разработчику, вручную дособирающему checkout под новую тему, донорские картинки уже доступны
   в файлах темы (`AssetPlacer`), заводить их ЕЩЁ и в CMS-медиатеку без привязки к какому-либо
   контенту не даёт ощутимой пользы.
5. ✅ **404**: `resources/views/errors/404.blade.php` заменён на тонкую стабильную оболочку
   (`@include('public.errors.404_content')`), прежний хардкод перенесён один в один в
   `resources/themes/base/views/public/errors/404_content.blade.php`. Новый `NotFoundPageBuilder`
   (аналог `DynamicPageTransplanter` по роли — не Page-запись, файл темы напрямую) для типа `404`:
   в отличие от static-страниц (`PageBuilder`) НЕ гоняет контент через ИИ/`SchemaBuilder` вовсе —
   у 404 нет `$seo`, редактируемые ACF-поля ему не нужны, поэтому донорский контент идёт в файл
   темы как есть (только перенос картинок в медиатеку через `AssetImporter` + возврат вырезанного
   `ArchiveParser`'ом `<h1>` литеральным текстом, а не `{{ $seo->name }}` — у 404 нет `$seo`).
   Оркестратор: новая явная ветка для `TYPE_404` в `ProcessThemeImportJob` (до
   `$transplantableTypes`, у `NotFoundPageBuilder` другой контракт — без параметра типа).
   **Живое тестирование**: до правки — прямой HTTP-запрос несуществующего URL на текущем сайте
   зафиксирован как baseline (200 контента/404 код, текст «Oops!…» на месте); после переноса
   хардкода в `base`-тему и замены оболочки — тот же запрос даёт идентичный результат (активная
   тема сайта — `custom`, у неё нет своего `errors/404_content.blade.php`, поэтому резолв идёт по
   фолбэку на `base`, как и предполагает `config/view.php` — реальное, а не гипотетическое
   подтверждение цепочки резолва). Отдельно, `NotFoundPageBuilder::build()` на `proper-loud/404.html`
   напрямую — картинка перенесена в медиатеку, `src` переписан, `<h1>` (с апострофом в тексте —
   проверка HTML-экранирования) корректно вставлен литералом. **Полный прогон через реальный
   `handle()` джобы**: 18 страниц (было 17 на шаге 3), 0 ошибок — `404.html` дал `status: created`,
   checkout/thanks — по-прежнему корректно `not_implemented`. Все тестовые данные (9 страниц, 34
   медиафайла, тема, запись импорта) удалены, счётчики вернулись к базовой линии 7/2735; финальный
   повторный HTTP-запрос несуществующего URL на проде подтвердил — 404-страница сайта не задета
   тестовым прогоном.
6. ✅ **product транспланter**: карточка/варианты/отзывы, ранее сознательно отложенные в шаге 3.
   Реальный `product.html` донора показал, что секция отзывов (`section-reviews`) — самый обычный
   репитер (текст/автор, никакой интерактивности) — заведена как `repeat_children`, тем же
   механизмом, что и у blog. Цена/варианты/кнопки покупки — наоборот, требуют реальных
   корзина/избранное/сравнение/переключатели-вариантов хуков, которых у донорской вёрстки нет и
   не может быть — для них новый вид листа **`kind: 'component'`** в `DynamicPageTransplanter::
   spliceLeaves()` (тот же принцип, что `repeat_component` у catalog, но для ОДИНОЧНОГО узла, не
   для инстанса repeater'а): заменяет СОДЕРЖИМОЕ найденного узла на готовый кусок разметки темы,
   а не на транскрибированное значение. Под это из `resources/themes/base/views/public/
   product.blade.php` вынесены три новых переиспользуемых partial'а —
   `layouts/product_price.blade.php`, `layouts/product_variations.blade.php`,
   `layouts/product_actions.blade.php` (были инлайн, теперь `@include`, порядок/содержимое
   побайтово сверены через `git diff` до коммита — не только по факту компиляции). Описание
   товара — `wysiwyg`-лист (`$product->description`), как и `body` у article.
   `heading`/`open_graph` у product переопределены на `$product->name`/фото товара, а не
   `$seo->name`/favicon (как у остальных типов) — тот же принцип точечных переопределений
   `wrap()`, что и у search/catalog.
   **Живые баги, пойманные на реальном доноре**:
   - `resolveSimpleSelectorToken()` поддерживал только ОДИН класс на токен селектора — модель
     иногда даёт узел с несколькими классами через точку (`div.product-page__info-buttons.sticky`
     — донор реально пишет `class="product-page__info-buttons sticky"`), токен целиком не
     матчился регэкспом, слот молча терялся. Тот же класс проблемы, что уже дважды ловился
     раньше (составные селекторы у blog, «голая» цепочка у search) — токен теперь разбирает ЛЮБОЕ
     число классов подряд (каждый — отдельный XPath `contains()`-предикат, И-семантика, как у
     CSS `.a.b`). Регрессия (один класс) и новый случай (несколько) проверены синтетически, 5/5.
   - При сборке через `npx mix` с ПОСТОРОННИМ `ACTIVE_THEME` (для сборки отдельной тестовой темы)
     Laravel Mix **перезаписывает** `public/mix-manifest.json` целиком, а не дополняет — унёс с
     собой уже собранные записи активной на сайте темы (`app`), сайт на секунды упал (`Unable to
     locate Mix file`). Не баг кода ThemeImport, чисто процедурная ошибка при живом тестировании —
     задокументировано как предостережение: сборку тестовой темы через `npx mix`/`ACTIVE_THEME=...`
     нельзя запускать, если на сайте прямо сейчас активна ДРУГАЯ тема — только в изолированном
     окружении, либо пересобирать активную тему сразу следом (как и было сделано здесь).
   Полный прогон через реальный `handle()` джобы: 20 страниц, 0 ошибок, оба product-донора
   (`product.html`, `strains-product.html`) — `status: created`. Обе сборки скомпилированы Blade-
   компилятором без ошибок; сборка ассетов (`npx mix`) прошла без НОВЫХ ошибок (единственная
   оставшаяся — уже известная несовместимость `chosen-js` с webpack, см. шаг 3, не относится к
   product-транспланту). Все тестовые данные (10 страниц, 19 медиафайлов, тема — включая
   `public/themes/live-test-product`, запись импорта) удалены.
7. ✅ **catalog: фильтры/сортировка** — ранее сознательно отложены в шаге 3 («только сетка +
   пагинация»). Разбор `resources/themes/base/js/shop/filters.js` показал: сортировка/фильтры/
   сетка/пагинация — ОДНА скоординированная AJAX-система на конкретных id/именах полей
   (`#catalogFilters` — форма с hidden `page`/`order`/`category`, `#catalogFilterFields`,
   `#catalogSelectedFilters`, `#catalogProducts`, `#catalogPagination`, `.js-catalog-sort-select`)
   — сплайсить их по отдельности бессмысленно, без ВСЕХ сразу AJAX никуда не запишет результат.
   **Решение (зафиксировано с пользователем)**: вся область (сайдбар + сетка + пагинация)
   заменяется ЦЕЛИКОМ на реальный рабочий блок темы — донорский визуальный дизайн ИМЕННО этой
   области теряется (показывается в стиле базовой темы), внешняя оболочка страницы донора (шапка,
   баннер, хлебные крошки) остаётся прежней. Из `catalog.blade.php` вынесен НОВЫЙ partial
   `layouts/catalog_filterable_area.blade.php` (весь `<div class="row">` — сайдбар с
   `#catalogFilters`/сортировкой/`@include('public.layouts.filters')` + `#catalogProducts`
   (`@include('public.layouts.products_list')`, тот же repeater-partial, что уже даёт carousel
   товаров) + `#catalogPagination`), извлечение сверено побайтово через `git diff`.
   Код транспланter'а менять не пришлось вовсе — новый слот `filterable_area` (`type: 'component'`)
   переиспользует УЖЕ существующий механизм одиночных component-листьев (введён в шаге 6 для
   product) без единой правки в `DynamicPageTransplanter`; в `DynamicSlotVocabulary`/
   `BladeExpressionMap` `repeat`/`repeat_component` catalog заменены на один `leaves`-слот.
   `search` не тронут — у неё в рабочей теме нет фильтров/сортировки вовсе, только сетка+пагинация
   (уже сделано в шаге 3), фильтровать нечего.
   **Живое тестирование**: прямой `build()` на 3 catalog-донорах (`catalog.html`, `strains.html`,
   `city.html`) — все три собрались, `catalog_filterable_area` подключён, донорская внешняя
   обёртка (`<div class="section page-catalog">` и т.п.) сохранена. Все три Blade-скомпилированы
   без ошибок. Полный прогон через реальный `handle()` джобы: 19 страниц, 3 catalog-донора
   успешно (`created`), четвёртый (`index.html` — реально главная страница донора, эвристика/ИИ
   и раньше ошибочно относили её к catalog) корректно вернул `no_slots_matched` — новый, более
   строгий словарь (нужен ИМЕННО filterable-area-образный узел, не просто повтор карточек) не стал
   натягивать несовпадающую разметку, безопасная деградация, не баг. Тестовые данные (9 страниц)
   удалены; медиафайлы в этом прогоне не создавались вовсе — `AssetImporter` дедуплицировал по
   md5-хэшу с уже существующими записями темы `app` (тот же донор, те же картинки).

## Открытые вопросы

- Название и место нового раздела в меню админки — по аналогии с «Шаблоны страниц» / «Импорт
  страниц» (`resources/data/menus/admin-menu.json`)?
- Полнота таксономии типов: у proper-loud были ещё `city`/`reviews`/`order-tracking`/
  `order-tracking-info`/`strains-product` — отдельные типы, вариации `product`/`checkout`, или
  тоже в `static`-фоллбек по умолчанию (безопасно, но менее полезно, чем отдельный транспланter)?
  Решать по мере появления повторяющихся паттернов на реальных архивах, не пытаться закрыть все
  варианты заранее.
- Порог уверенности эвристики, после которого включается ИИ-классификация — жёсткий список
  паттернов имён или нечто более гибкое (например по совпадению с уже известными site-side route
  именами `catalog`/`product`/`search`/`blog`/`checkout` через `Str::contains`)?
- Экран ревью нового раздела: как показывать «какая тема сгенерирована из какого архива» + кнопка
  активации — расширение существующего `admin.page_imports.review` по аналогии, или полностью
  отдельный view?